{
  "name": "titan-sdk",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "pnpm -r --filter ... run build",
    "test": "pnpm -r --filter ... run test",
    "lint": "pnpm -r --filter ... run lint"
  }
}
